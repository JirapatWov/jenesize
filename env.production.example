# Production Environment Variables Example
# Copy this file and fill in your actual values
# DO NOT commit the actual .env.production file!

# ======================
# Database (Supabase)
# ======================
# Get from: Supabase Dashboard → Settings → Database → Connection String (URI)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# ======================
# JWT Secrets
# ======================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-min-32-characters-change-this"
JWT_EXPIRES_IN="1h"

REFRESH_TOKEN_SECRET="your-refresh-token-secret-min-32-characters-change-this"
REFRESH_TOKEN_EXPIRES_IN="7d"

# ======================
# API Configuration
# ======================
NODE_ENV="production"
API_PORT="10000"

# Your Vercel frontend URL (update after Vercel deployment)
WEB_URL="https://your-app.vercel.app"

# ======================
# Redis (Render)
# ======================
# Get from: Render Dashboard → Redis Instance → Connection
REDIS_HOST="your-redis-instance.render.com"
REDIS_PORT="6379"
REDIS_PASSWORD="your-redis-password"
REDIS_URL="redis://:your-redis-password@your-redis-instance.render.com:6379"

# ======================
# Frontend (Vercel)
# ======================
# Your Render API URL (update after Render deployment)
NEXT_PUBLIC_API_URL="https://affiliate-api.onrender.com"

# ======================
# Optional: Monitoring & Analytics
# ======================
# SENTRY_DSN=""
# GOOGLE_ANALYTICS_ID=""
# PLAUSIBLE_DOMAIN=""

# ======================
# Optional: Email Service
# ======================
# SMTP_HOST=""
# SMTP_PORT=""
# SMTP_USER=""
# SMTP_PASS=""
# EMAIL_FROM=""

# ======================
# Instructions
# ======================
# 
# 1. Copy this file to create your production config:
#    cp env.production.example .env.production
#
# 2. Fill in all the values with your actual credentials
#
# 3. For Render (Backend):
#    - Add these variables in Render Dashboard → Environment tab
#    - Do NOT commit .env.production to git
#
# 4. For Vercel (Frontend):
#    - Add NEXT_PUBLIC_API_URL in Vercel Dashboard → Settings → Environment Variables
#
# 5. Generate secure secrets:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 6. Test locally first:
#    export $(cat .env.production | xargs)
#    pnpm dev
#

